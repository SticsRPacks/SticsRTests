name: integration-tests

on:
  workflow_call:
    inputs:
      repo:
        description: Repository under test
        required: true
        type: string
      ref:
        description: Branch or tag for the repository under test
        required: false
        default: ""
        type: string
      sha:
        description: Exact commit SHA for the repository under test
        required: false
        default: ""
        type: string
      dependency_mode:
        description: Ref selection mode for the other SticsRPacks packages
        required: true
        type: string
      sticsrtests_ref:
        description: Branch, tag, or SHA of SticsRTests to run
        required: false
        default: main
        type: string

permissions:
  contents: read

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ github.token }}

    steps:
      - name: Checkout SticsRTests
        uses: actions/checkout@v4
        with:
          repository: SticsRPacks/SticsRTests
          ref: ${{ inputs.sticsrtests_ref }}
          fetch-depth: 0

      - name: Resolve package refs
        id: package-refs
        env:
          PACKAGE_UNDER_TEST: ${{ inputs.repo }}
          PACKAGE_REF: ${{ inputs.ref }}
          PACKAGE_SHA: ${{ inputs.sha }}
          DEPENDENCY_MODE: ${{ inputs.dependency_mode }}
        run: |
          set -euo pipefail

          resolve_release_ref() {
            local pkg="$1"
            local ref

            ref="$(git ls-remote --refs --tags "https://github.com/SticsRPacks/${pkg}.git" \
              | awk -F/ '
                  {
                    tag = $NF
                    if (tag ~ /^v?[0-9]+([.][0-9]+)+([.-][0-9A-Za-z]+)*$/) {
                      key = tag
                      sub(/^v/, "", key)
                      print key "\t" tag
                    }
                  }
                ' \
              | sort -V \
              | tail -n 1 \
              | cut -f2)"

            if [ -z "${ref}" ]; then
              echo "No release tag found for ${pkg}" >&2
              exit 1
            fi

            printf '%s\n' "${ref}"
          }

          resolve_ref() {
            local pkg="$1"

            if [ "${pkg}" = "${PACKAGE_UNDER_TEST}" ]; then
              if [ -n "${PACKAGE_SHA}" ]; then
                printf '%s\n' "${PACKAGE_SHA}"
                return
              fi

              if [ -n "${PACKAGE_REF}" ]; then
                printf '%s\n' "${PACKAGE_REF}"
                return
              fi

              printf 'main\n'
              return
            fi

            if [ "${DEPENDENCY_MODE}" = "main" ]; then
              printf 'main\n'
              return
            fi

            if [ "${DEPENDENCY_MODE}" = "release" ]; then
              resolve_release_ref "${pkg}"
              return
            fi

            echo "Unsupported dependency mode: ${DEPENDENCY_MODE}" >&2
            exit 1
          }

          {
            printf 'repo,ref\n'

            for pkg in SticsRPacks CroPlotR CroptimizR SticsRFiles SticsOnR; do
              printf '%s,%s\n' "${pkg}" "$(resolve_ref "${pkg}")"
            done
          } > package-refs.csv

          cat package-refs.csv

          {
            echo 'extra_packages<<EOF'
            tail -n +2 package-refs.csv | while IFS=, read -r repo ref; do
              if [ "${repo}" = "${PACKAGE_UNDER_TEST}" ]; then
                continue
              fi
              printf 'SticsRPacks/%s@%s\n' "${repo}" "${ref}"
            done
            echo 'EOF'
            echo "caller_ref=$(awk -F, -v repo=\"${PACKAGE_UNDER_TEST}\" '$1 == repo { print $2 }' package-refs.csv)"
          } >> "${GITHUB_OUTPUT}"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "17"

      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Set up Pandoc
        uses: r-lib/actions/setup-pandoc@v2

      - name: Install system libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libxslt1-dev libcurl4-openssl-dev
          wget http://nz2.archive.ubuntu.com/ubuntu/pool/main/o/openssl/libssl1.1_1.1.1f-1ubuntu2_amd64.deb
          sudo dpkg -i libssl1.1_1.1.1f-1ubuntu2_amd64.deb
          wget http://http.us.debian.org/debian/pool/main/o/openldap/libldap-2.4-2_2.4.57+dfsg-3+deb11u1_amd64.deb
          sudo apt-get install -y ./libldap-2.4-2_2.4.57+dfsg-3+deb11u1_amd64.deb
          wget http://archive.ubuntu.com/ubuntu/pool/main/i/icu/libicu66_66.1-2ubuntu2_amd64.deb
          sudo dpkg -i libicu66_66.1-2ubuntu2_amd64.deb

      - name: Set up R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          dependencies: FALSE
          pak-version: rc
          extra-packages: |
            any::testthat
            any::remotes
            any::dplyr
            any::xfun
            any::knitr
            ${{ steps.package-refs.outputs.extra_packages }}

      - name: Install caller package
        env:
          PACKAGE_UNDER_TEST: ${{ inputs.repo }}
          PACKAGE_REF: ${{ steps.package-refs.outputs.caller_ref }}
        run: |
          remotes::install_github(
            sprintf("SticsRPacks/%s@%s", Sys.getenv("PACKAGE_UNDER_TEST"), Sys.getenv("PACKAGE_REF")),
            dependencies = FALSE,
            upgrade = "never"
          )
        shell: Rscript {0}

      - name: Install local package
        run: |
          remotes::install_local(".", dependencies = FALSE, upgrade = "never")
        shell: Rscript {0}

      - name: Run integration tests
        run: testthat::test_package("SticsRTests")
        shell: Rscript {0}
